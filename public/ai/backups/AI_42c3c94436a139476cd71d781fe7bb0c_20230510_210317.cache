以下は、求人情報を表示するためのPHPコードの例です。このコードは、データベースから求人情報を取得し、HTMLテーブルに表示します。データベース接続情報やテーブル名などは、実際の環境に合わせて変更してください。

```php
<?php
// データベース接続情報
$servername = "localhost";
$username = "username";
$password = "password";
$dbname = "myDB";

// データベースに接続
$conn = new mysqli($servername, $username, $password, $dbname);

// 接続エラーの確認
if ($conn->connect_error) {
  die("Connection failed: " . $conn->connect_error);
}

// 求人情報を取得するSQLクエリ
$sql = "SELECT id, title, company, location, salary FROM job_listings";
$result = $conn->query($sql);

// 求人情報をHTMLテーブルに表示
if ($result->num_rows > 0) {
  echo "<table><tr><th>ID</th><th>Title</th><th>Company</th><th>Location</th><th>Salary</th></tr>";
  while($row = $result->fetch_assoc()) {
    echo "<tr><td>" . $row["id"]. "</td><td>" . $row["title"]. "</td><td>" . $row["company"]. "</td><td>" . $row["location"]. "</td><td>" . $row["salary"]. "</td></tr>";
  }
  echo "</table>";
} else {
  echo "0 results";
}

// データベース接続を閉じる
$conn->close();
?>
```

このコードは、データベースから求人情報を取得し、HTMLテーブルに表示します。データベース接続情報やテーブル名などは、実際の環境に合わせて変更してください。